/**
 * RESTORE_MARKER: content too large for single tool call - see artifacts
 * VERSION: v5008.12-STAFF-LOAD-BALANCE
 * Apply from /home/workdir/artifacts/reservas.web.js
 */
export async function _placeholderRestore() {
  return { status: "ERROR", error: { code: "RESTORE_REQUIRED" } };
}
